<div class='debug-output'>
  <%= debug(params) if Rails.env.development? %>

  <% if Rails.env.development? %>
    <code>
      Chef Id =
      <%= session[:chef_id] %>
    </code>
    <code>
      Is current chef an admin?
      <%= current_chef&.admin? %>
    </code>
  <% end %>
</div>